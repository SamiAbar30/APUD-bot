# Implementation plan
1. Parent: scaffold Node/TypeScript configuration, Prisma schema/migration, isolated Docker Postgres/Redis, typed env; install dependencies and generate client.
2. Claude Code: implement domain FSM, 11-field decision engine, PDF/certificate identity checks, geography lookup, real-input validation harness. Read docs/ARCHITECTURE.md for interfaces.
3. Gemini: implement typed HTTP connectors for WhatsApp, Kmaleon verified writes, partner financial gate, configured Sede draft runner. Report uncertain remote contracts explicitly.
4. Parent: connect authenticated API/operator dashboard, HMAC webhooks, transactional inbox/outbox, debounce, per-case lock and worker retry/reconciliation/DLQ.
5. Parent: integrate agent output, strict build and independent review for lost events, race conditions, secret persistence and unsafe side effects. Fix findings.
6. Parent: run against isolated real PostgreSQL/Redis; run real HTTP health/API checks and document fixture availability. Create deployment runbook and separate questions list; report external dependencies accurately.
