You are the Gemini implementation subagent explicitly requested by user. Build now without questions. Read docs/ARCHITECTURE.md for shared contract and docs/source/codex_apoderamientos_master_prompt.md plus transcript. You own ONLY src/adapters/**, src/contracts/whatsapp.contract.ts, src/contracts/kmaleon.contract.ts, src/contracts/apudata.contract.ts, docs/agents/gemini-interfaces.md. Other agents are concurrently building domain/infrastructure; do not revert their edits, touch package/config/Prisma, or commit. Implement strongly typed standalone adapters with constructor options, independent of global env and Prisma. WhatsApp: real Graph HTTP APIs, raw HMAC validation constant time, bounded download MIME/size/SHA256 checks and SSRF-host restriction, text/buttons/template/media send, normalize webhook metadata WITHOUT persisting freeform text/password; arbitrary text must not go to logs/DB/Redis. Kmaleon: no invented vendor protocol success. You may read existing /Users/litigiosmacmini/RECLAMACION-BOT/src files to discover actual RPC contract but never secrets/.env or live systems. Build configurable RPC auth + upload/read-back, matching immutable content hash, project and idempotency reference, never filename-only; timeouts as uncertain outcome and reconcile before retry; notice verification. Apudata: provider domain/API undocumented; configurable explicit reviewed endpoint mappings and schemas, mark unverified dependency, financial gate preapproval true + expiry + evidence + approved real IBAN/amount, idempotent order and HMAC timestamped callback validation. Never invent a bank account or enable provider by default. Sede Playwright: implement configured reviewed selector recipe for draft creation with memory-only PFX and scoped explicit consent, approved official HTTPS origins, downloads memory if possible. No invented live selectors or autonomous legal signing/revocation. A configured real implementation can be gated, but no pretending a throw-only stub completes it. No email access, secret reads, external messages or writes, deployments. NO MOCK TESTS/SYNTHETIC DATA. Do not contact live CRM or court. Use fetch/undici, zod, playwright as needed. Preserve package. Provide doc with exact interfaces/examples for parent orchestration and explicit unavailable facts. No questions, proceed and finish code.
