You are the Claude Code implementation subagent requested by the user. Build now without questions. Read docs/ARCHITECTURE.md (binding integration interface) and docs/source/codex_apoderamientos_master_prompt.md plus workflow transcript. You own ONLY src/domain/**, src/contracts/decision.contract.ts, src/core/decision-engine.ts, src/core/pdf-auditor.ts, src/core/cert-inspector.ts, src/core/geo-normalizer.ts, scripts/verify-real.ts, test/fixtures/README.md and docs/agents/claude-interfaces.md. Implement them completely with production error handling. Other agents are concurrently building adapters and infrastructure; do not revert others or edit package.json, tsconfig, Prisma or API. Use installed dependencies when ready. No need to use skills requiring approval: user explicitly requests immediate autonomous implementation. No email access (token not supplied), no secret/env reads, no external messages, live CRM/court writes or deployments. NO MOCKS, NO SYNTHETIC FIXTURES/TEST DATA; real test runner must fail clearly if authorized real inputs manifest absent, never skip into false success. Read the shared interface precisely. Design events to cover all branches plus audit approval, client review, verified CRM and notifications. Provide exported enums and types with documentation. Tight identity checks and mandatory human approval must block automatic upload before approval, false positive Airam/negated powers must not count valid. Certificate key/cert match, date, DNI, bounded RAM use and wipe buffers; document unavoidable JS string copy limitation. Geo deterministic lookup catalog only, LLM cannot invent districts. Keep code clean, strict, no TODO/fake implementations. Run focused typecheck when possible but don't fix files owned by others. Write brief interface report with usage and uncertainties to docs/agents/claude-interfaces.md. Finish by reporting files/results. No git commits.
